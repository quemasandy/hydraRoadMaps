/** Encryption at Rest */ import { createCipheriv, createDecipheriv, randomBytes } from 'crypto'; export class EncryptionAtRest { static demonstrate() { const key = randomBytes(32); const iv = randomBytes(16); const cipher = createCipheriv('aes-256-cbc', key, iv); let encrypted = cipher.update('sensitive data', 'utf8', 'hex'); encrypted += cipher.final('hex'); console.log('\n=== Encryption at Rest ==='); console.log('Encrypted:', encrypted); }} if (require.main === module) { EncryptionAtRest.demonstrate(); }