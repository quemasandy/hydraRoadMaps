/** PKCE */ import { createHash, randomBytes } from 'crypto'; export class PKCEDemo { static demonstrate() { console.log('\n=== PKCE ===\n'); const verifier = randomBytes(32).toString('base64url'); const challenge = createHash('sha256').update(verifier).digest('base64url'); console.log('Code Verifier:', verifier.substring(0, 20) + '...'); console.log('Code Challenge:', challenge); }} if (require.main === module) { PKCEDemo.demonstrate(); }