/** JWT Validation */ import { createHmac } from 'crypto'; export class JWTValidator { static validate(token: string, secret: string): boolean { const [header, payload, signature] = token.split('.'); const data = header + '.' + payload; const expected = createHmac('sha256', secret).update(data).digest('base64url'); return signature === expected; }} if (require.main === module) { console.log('JWT validation ready'); }